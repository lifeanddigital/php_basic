asrray()構文を使って一気に配列を初期化する方法
次に、array()構文を使って配列を一気に初期化する方法を見てみましょう。
一般的にはキーと値を指定して配列を作成します。しかし、キーを省略して、配列を作成することもできます。
この場合は、0,1,2...と自動的に連番のきーを割り振ります。

[書式]array()文を使った配列の初期化
$変数　=　array(キー1　=>値1、キー2　=>値2,キー3=>値3　...);
$変数　＝　array(値1,値2,値3　...);
では、array()構文を使って、睦月から師走まで月名を一気に初期化する例で使い方を確認しましょう。