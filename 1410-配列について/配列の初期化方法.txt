配列の初期化方法

はじめに配列の個々の要素に値を代入する初期化方法を紹介します。
変数名の後ろにキーを付けて値を代入することで、、配列を初期化し値を設定できます。

書式：　配列の初期化

$変数[キー]　=　値;

以下は、配列に値を代入して、配列を初期化する例です。ちなみに、文字列はのキーを持つ配列を「連想配列」と呼びます。
ちなみに、print_r（）メソッドを使うと、配列のキーと値を見やすく表示することができます。


　
